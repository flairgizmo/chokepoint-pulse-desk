# Ask Grok (xAI). Never prefix this with VITE_ — it must stay on the server.
# Create a key at https://console.x.ai then paste it here for local `npm run dev`.
# On Netlify: Site settings → Environment variables → XAI_API_KEY
XAI_API_KEY=
XAI_MODEL=grok-3-mini
